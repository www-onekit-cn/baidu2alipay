<image
class="onekit-image {{onekitClass}}"
style="{{onekitStyle}}"
id="{{onekitId}}"

src="{{src}}"
mode="{{mode}}"
lazy-load="{{lazyLoad}}"

onLoad="image_load"
onError="image_error"
onLongTap="image_longTap"
/>
