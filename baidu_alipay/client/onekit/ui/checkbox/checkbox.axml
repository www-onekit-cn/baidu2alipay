<view>
<checkbox 
  class="onekit-checkbox {{onekitClass}}"
  style="{{onekitStyle}}"
  id="{{onekitId}}"
  value="{{value}}" 
  disabled="{{disabled}}"
  checked="{{checked}}"
  color="{{color}}"
  onChange="{{onChange}}}"/>
<slot/>
</view>