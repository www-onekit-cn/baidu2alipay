import { SCOPE, PURPOSE, OPERATION } from '../constant';
declare class Spec {
    name: string;
    keys: string[];
    scope: SCOPE;
    purpose: PURPOSE;
    operation: OPERATION;
    schema: {
        [key: string]: any;
    };
    constructor(name: string, spec: any);
    getArgsFromArray(args: any): any;
}
export default Spec;
