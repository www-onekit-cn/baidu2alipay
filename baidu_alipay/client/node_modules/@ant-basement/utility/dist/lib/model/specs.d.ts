import Spec from './spec';
declare const OP_SPECS: {
    [key: string]: Spec;
};
declare const OP_NAMES: string[];
declare function isOpValid(op: string | number | symbol): boolean;
declare function isCollectionOp(op: string | number | symbol): boolean;
declare function isDatabaseOp(op: string | number | symbol): boolean;
export { OP_SPECS, OP_NAMES, isOpValid, isCollectionOp, isDatabaseOp };
