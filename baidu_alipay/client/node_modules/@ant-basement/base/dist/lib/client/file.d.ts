import BaseClient from './base';
declare class FileClient extends BaseClient {
    constructor(builder: any);
    deleteFile(url: string): Promise<any>;
    getPrivateFileByUrl(url: string, expires?: number): Promise<any>;
    uploadFile(_: any): Promise<void>;
    validateFilename(name: string): void;
}
export default FileClient;
