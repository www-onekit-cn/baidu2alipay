declare class BaseClient {
    protected logger: any;
    private builder;
    private service;
    constructor(builder: any, service: any);
    protected request(...args: any[]): Promise<any>;
}
export default BaseClient;
