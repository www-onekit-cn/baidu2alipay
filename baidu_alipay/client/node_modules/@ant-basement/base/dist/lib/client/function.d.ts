import BaseClient from './base';
declare class FunctionClient extends BaseClient {
    constructor(builder: any);
    invoke(handler: string, data?: object): Promise<any>;
}
export default FunctionClient;
