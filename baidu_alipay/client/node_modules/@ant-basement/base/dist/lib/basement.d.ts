import DbClient from './client/db';
import FileClient from './client/file';
import FunctionClient from './client/function';
import { BasementOptions } from './typing';
declare class Basement {
    protected _db: DbClient;
    protected _file: FileClient;
    protected _function: FunctionClient;
    protected urllib: any;
    protected options: BasementOptions;
    protected logger: any;
    constructor(options: BasementOptions);
    readonly db: DbClient;
    readonly file: FileClient;
    readonly ['function']: FunctionClient;
    createTraceId(): string;
    request(requestOptions: any): Promise<any>;
    toString(): BasementOptions;
    protected transform(..._: any[]): void;
}
export default Basement;
